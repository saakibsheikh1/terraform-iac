# Compute Module

Reusable Terraform module for creating an EC2 instance.

## Inputs

- `project_name`
- `environment`
- `ami_id`
- `instance_type`
- `subnet_id`
- `security_group_id`

## Outputs

- `instance_id`
- `public_ip`